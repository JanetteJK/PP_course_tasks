Orientation writing  tasks 1.2
1.
a) Because these are main events that move the line along. Some of the in between events can’t happen before another main event is scheduled.
These are the ‘main clock moving events’
b) Initial B event(s) should be generated in the event schedule at the start of the service point.
After which the program can go through the c events. The initial states are also generated at the beginning.
